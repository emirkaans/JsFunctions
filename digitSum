// Basamak Sayılarını Toplayan Fonksiyon

const calcSum = function(num){
let sum = 0;

while(true){

	if(Math.floor(num / 10) >= 1){
  	sum += num % 10 
  	num = Math.floor(num/10);
   } else {
   sum += num;
   break;
  }
 }
  return Math.floor(sum);
}

